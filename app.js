const rupiah=v=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(v||0);
async function loginNasabah(){const nama=document.getElementById('nama').value.trim();if(!nama)return alert('Masukkan nama nasabah');const {data,error}=await supabase.from('nasabah').select('*').eq('nama_nasabah',nama).single();if(error||!data)return alert('Nama tidak ditemukan');loginCard.classList.add('hidden');dashboard.classList.remove('hidden');namaText.textContent=data.nama_nasabah;va.textContent=data.nomor_va;nominal.textContent=rupiah(data.nominal_tagihan);limit.textContent=rupiah(data.limit_pinjaman);status.textContent=data.status_pembayaran;}
async function konfirmasiBayar(){await supabase.from('nasabah').update({status_pembayaran:'Menunggu Konfirmasi'}).eq('nama_nasabah',namaText.textContent);status.textContent='Menunggu Konfirmasi';alert('Konfirmasi pembayaran berhasil dikirim');}
let tap=0;logo.onclick=()=>{tap++;if(tap===3){tap=0;const pin=prompt('PIN Admin');if(pin==='061223')alert('Admin mengelola data di Supabase Table Editor.')}};
